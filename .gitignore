# ESPHome secrets — never commit these
secrets.yaml

# Backup files
*.bak
*.backup

# macOS
.DS_Store

# Local project context — not for public repo
CONTEXT.md

# Experimental / scratch YAMLs — never commit
papercolor-exp.yaml
